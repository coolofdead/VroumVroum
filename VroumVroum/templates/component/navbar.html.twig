{% block navbar %}

    <script src="{{ asset('js/basket.js') }}"></script>

    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand" href="/">VroumVroum</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item">
                    <a class="nav-link active" href="#">🧙🏼‍♂️ Compte</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link active" href="#">🧾 Historique des commandes</a>
                </li>
                {% if app.user != null %}
                    <li class="nav-item">
                        <a class="nav-link active" href="#">🔴 Déconnexion</a>
                    </li>
                    {% if is_granted('ROLE_ADMIN') or is_granted('ROLE_RESTAURATEUR') %}
                        <li class="nav-item">
                            <a class="nav-link active" href="#">🍴 Restaurants</a>
                        </li>
                    {% endif %}
                {% else %}
                    <li class="nav-item">
                        <a class="nav-link active" href="#">🟢 Connexion</a>
                    </li>
                {% endif %}
            </ul>
            <div class="nav-item my-2">
                {% include "component/basket.html.twig" %}
            </div>
        </div>
    </nav>

    

{% endblock %}